export default function Map() {
    return (
        <div style={{ maxWidth: 'var(--max-content-width)', margin: '0 auto', padding: 'var(--space-5) var(--space-4)' }}>
            <h1 style={{ fontFamily: 'var(--font-serif)', color: 'var(--primary)' }}>Explore</h1>
            <p style={{ color: 'var(--subtext)' }}>
                A map of nearby events and book-swap points will live here.
            </p>
        </div>
    );
}
