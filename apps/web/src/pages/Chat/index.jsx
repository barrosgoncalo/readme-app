export default function Chat() {
    return (
        <div style={{ maxWidth: 'var(--max-content-width)', margin: '0 auto', padding: 'var(--space-5) var(--space-4)' }}>
            <h1 style={{ fontFamily: 'var(--font-serif)', color: 'var(--primary)' }}>Community</h1>
            <p style={{ color: 'var(--subtext)' }}>
                One chat per event will live here — message other attendees and see moderation from Back
                Officers.
            </p>
        </div>
    );
}
