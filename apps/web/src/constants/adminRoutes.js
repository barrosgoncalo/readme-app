// Admin route paths. Separate from WEB_ROUTES since the admin area
// has its own shell (AdminShell) and no user-facing nav.
export const ADMIN_ROUTES = {
    DASHBOARD: '/admin/dashboard',
    USERS: '/admin/users',
    REPORTS: '/admin/reports',
    PUBLICATIONS: '/admin/publications',
    SETTINGS: '/admin/settings',
    LOGS: '/admin/logs',
};
